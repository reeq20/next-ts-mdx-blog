import {Head} from "next/document";
import Layout from "../../../components/layouts/PostLayout";

export const meta = {
    title: "dev",
    date: "2021-01-03",
    desc: "これはサンプルテキストです。",
};

mark down text
これはサンプルテキストです。文字数や行間を確認するため入力しています。


```test.jsx
const test = 'abc';
console.log('test');

const t = async()=>{
const r = await method('a');
return r;
}

```


<div>
    test
</div>

<div>
    test
</div>

<div>
    test
</div>

# h1

ここにテキスト

## h2

export default ({children}) => (
    <Layout meta={meta}>
        {children}
    </Layout>
);
